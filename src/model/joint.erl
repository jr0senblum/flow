%%% ----------------------------------------------------------------------------
%%% JOINT used in a range-of-motion description.
%%% ----------------------------------------------------------------------------

-module(joint, [Id, Name::string()]).
-compile(export_all).
